# Artwork in this bundle

`Media.xcassets/Mascot.imageset/mascot.svg` is a copy, not the original.

The artwork lives in `assets/` at the repository root and is edited there. `make sync-assets`
copies it here, and `make mac` runs that first, so the catalog cannot quietly drift from the
drawing that `assets/README.md` describes.
